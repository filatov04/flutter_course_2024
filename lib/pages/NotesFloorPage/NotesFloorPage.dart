import 'package:flutter/material.dart';

class NotesFloorPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Страница с заметками'),
    );
  }
}